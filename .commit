Version 2.4.153

* AbstractView.java (selectAll()): Helper method to select all text in
  an EditText widget.
* AbstractView.java (editorAction()): Sets the OnEditorActionListener
  instance to a TextView descendant.
* AbstractView.java (checkedChange()): Sets the OnCheckedChangeListener
  instance to a CompoundButton.
* sfView.java (release()): Add some check to not include the same
  instance twice in the cache.
* AbstractView.java (reset(Activity, View)): Found bug getting the wrong
  Activity to set.
