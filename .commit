Version 2.4.66

- CAndroidPagerView.java (setSelection()): Bug, calling a method of member
  'm_scroller' when is possible that it is still null.
- CAndroidPagerView.java (setVisibleView()): Same bug as before.
